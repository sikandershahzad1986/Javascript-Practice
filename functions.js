// lets create a function with a name adder

function adder(n1, n2) // will accept two numbers
{

    return n1+n2; // will add two numbers and will return the result
}

// now lets call the function

const result = adder(20,15); // we have provided two numbers 5 and 10

// now lets print the result

console.log(result);