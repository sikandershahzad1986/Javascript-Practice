// to do program in javascript

const tasks =[];

tasks.push('Breakfast');
tasks.push('Office');
tasks.push('Work');
tasks.push('Home');
tasks.push('Lunch');

tasks.forEach(function(task,index){

    console.log(`Task No. ${index+1} ${task}`);
});